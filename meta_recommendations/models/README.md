# Saved Models

This folder can store persisted recommendation model weights or checkpoints. The current
implementation trains the logistic regression model on demand, so no binary artifacts are
checked into source control.
